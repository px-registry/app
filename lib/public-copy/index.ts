// Block #6 frozen public copy — barrel. Leaf, node-safe (no DOM / no network):
// board surfaces import the constant and render it (single source of truth).

export {
  BLOCK6_COPY,
  allBlock6CopyStrings,
  BLOCK6_FORBIDDEN,
  type Block6Block,
  type Block6Key,
} from "./block6.ts";
