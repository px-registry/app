// Shared footer — one line, identical across the top page and every category
// page. PX takes no fee; activity is verifiable with standard tools.
export function Footer() {
  return (
    <footer className="foot">
      PX takes no fee on listed activity. Verifiable via standard tools.
    </footer>
  );
}
